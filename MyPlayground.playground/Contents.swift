import UIKit
import SwiftUI

func incrementCount(count: Int, str: String ) {
    Text(cc.count)
    Text(cc.str)
}

struct cc {
    var count {
        count += 1
    }
    var str = ""
}

var greeting = "Hello, playground"
